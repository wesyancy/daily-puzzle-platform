{
    "name": "@repo/game-engine",
    "version": "0.0.1",
    "private": true,
    "main": "./src/index.ts",
    "scripts": {
        "test": "vitest run"
    },
    "dependencies": {
        "@repo/dictionary": "workspace:*",
        "@repo/game-engine": "workspace:*"
    },
    "devDependencies": {
        "vitest": "^4.1.10"
    }
}